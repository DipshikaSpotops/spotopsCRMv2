server {
  listen 80;
  listen [::]:80;
  server_name spotops360.com www.spotops360.com;

  # Redirect HTTP to HTTPS (will be enabled after SSL setup)
  # Uncomment after SSL certificate is installed:
  # return 301 https://$server_name$request_uri;

  # --- Security ---
  location ~ /\. { deny all; }

  # --- API proxy ---
  location ^~ /api/ {
    proxy_pass http://127.0.0.1:5000;
    proxy_http_version 1.1;

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_intercept_errors off;
  }

  # --- WebSocket / Socket.IO (if used) ---
  location /socket.io/ {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_pass http://127.0.0.1:5000;
  }

  # --- Frontend (React Vite build) ---
  root /var/www/spotopsCRMv2/client/dist;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}

# SSL Configuration (uncomment after SSL certificate is installed)
# server {
#   listen 443 ssl http2;
#   listen [::]:443 ssl http2;
#   server_name spotops360.com www.spotops360.com;
#
#   ssl_certificate /etc/letsencrypt/live/spotops360.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/spotops360.com/privkey.pem;
#   ssl_protocols TLSv1.2 TLSv1.3;
#   ssl_ciphers HIGH:!aNULL:!MD5;
#   ssl_prefer_server_ciphers on;
#
#   # --- Security ---
#   location ~ /\. { deny all; }
#
#   # --- API proxy ---
#   location ^~ /api/ {
#     proxy_pass http://127.0.0.1:5000;
#     proxy_http_version 1.1;
#
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#
#     proxy_intercept_errors off;
#   }
#
#   # --- WebSocket / Socket.IO (if used) ---
#   location /socket.io/ {
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#     proxy_pass http://127.0.0.1:5000;
#   }
#
#   # --- Frontend (React Vite build) ---
#   root /var/www/spotopsCRMv2/client/dist;
#   index index.html;
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
# }


